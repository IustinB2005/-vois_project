version: '3'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - ACCESS_TOKEN_EXPIRE_MINUTES=30
      - SECRET_KEY=cheie_secreta_super_sigura_12345
      - ALGORITHM=HS256

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    